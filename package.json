{
  "name": "opencode-digest",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "./run.sh",
    "launchd:install": "cp -f com.47ng.opencode-digest.plist ~/Library/LaunchAgents/",
    "launchd:load": "launchctl load ~/Library/LaunchAgents/com.47ng.opencode-digest.plist",
    "launchd:unload": "launchctl unload ~/Library/LaunchAgents/com.47ng.opencode-digest.plist",
    "launchd:reload": "bun run launchd:unload && bun run launchd:load"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.1.65",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
